version: '3'

services:
  hacker:
    build:
      context: .
    command: |
      "sleep 15"
      "curl bwapp/install.php?install=yes"
    depends_on:
      - bwapp
    volumes:
      - ./app:/root/app
      - ./cves:/root/cves
      - ./db:/root/db
  bwapp:
    image: "raesene/bwapp"
    hostname: bwapp.com
    ports:
      - 8080:80
    expose:
      - 80

